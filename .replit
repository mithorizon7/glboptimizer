# This is the complete, corrected .replit file.
# The command on line 22 has been updated to use `uv pip sync pyproject.toml`.

modules = ["python-3.11", "nodejs-20", "python3", "postgresql-16"]

[nix]
channel = "stable-24_05"
packages = ["openssl", "postgresql", "redis"]

[deployment]
deploymentTarget = "autoscale"
run = ["sh", "-c", "uv pip sync pyproject.toml && gunicorn --bind 0.0.0.0:5000 --reuse-port --reload wsgi:application"]

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
# This is the critical line that has been fixed.
# It now tells `uv pip sync` to use the pyproject.toml file.
args = "uv pip sync pyproject.toml && gunicorn --bind 0.0.0.0:5000 --reuse-port --reload wsgi:application"
waitForPort = 5000

[[ports]]
localPort = 5000
externalPort = 80

[[ports]]
localPort = 5001
externalPort = 3001

[[ports]]
localPort = 6379
externalPort = 3000
